stages:
  - test

test:
  stage: test
  before_script:
    - export PATH="$HOME/.rbenv/bin:$PATH"
    - eval "$(rbenv init -)"
    - gem install bundler
    - rbenv rehash
    - bundler install
  script:
    - bundler exec kitchen test
